{
  "name": "profile-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "stats": "tsx scripts/generate-stats.ts",
    "render": "tsx scripts/generate-profile.ts",
    "build": "npm run stats && npm run render"
  },
  "devDependencies": {
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "@types/node": "^22.15.0"
  }
}
